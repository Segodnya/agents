---
name: audit-reply
description: >
  Разбирает замечания аудитора (ревьюера) и по каждому помогает решить: внести правку в код
  или подготовить ответ-обоснование, почему правка не нужна. Источники замечаний: открытые
  треды GitLab MR через `glab`, файл-отчёт (`.md` от review-staged или выгрузка комментариев),
  либо текст замечаний прямо в инвокации. Изучает дифф ветки и соседний код локально, даёт
  оценку и рекомендацию, применяет согласованные правки в рабочем дереве, собирает черновики
  ответов (в GitLab ничего не постит). Флаги `--auto` / `--commit` / `--base` — для `mr-ready`.
  Используй, когда пользователь даёт ссылку на GitLab MR, файл с ревью-замечаниями или список
  комментариев и просит «разобрать комментарии», «ответить аудитору», «что делать с замечаниями
  ревью», «пройтись по комментам MR», «разбери отчёт ревью», «вот замечания — что править, что
  объяснять», «audit reply», «/audit-reply», «look at the comments in the MR … decide fix or
  clarify» — и вообще всегда, когда речь о реакции на ревью-замечания, даже если слова «аудитор»
  или «ответ» не произнесены.
trigger: /audit-reply
---

# Audit Reply

По каждому замечанию решить с пользователем: **FIX** (правим код) или **REPLY** (обоснование без
правки). Правки — только после явного согласия.

- **Вход**: ссылка на MR (`https://host/group/project/-/merge_requests/41` или `project + iid +
  hostname`) / путь к `.md`|`.txt` / текст в инвокации. Репозиторий склонирован локально.
- **Выход (draft-only)**: правки в рабочем дереве + черновики ответов. В GitLab ничего не постить
  и не резолвить. Не делать `git add`/`commit`/`push`.
- **Нужно**: локальный чекаут того же проекта; для MR — `glab`, авторизованный на нужном инстансе
  (ошибка → подсказать `glab auth status|login --hostname <host>`), и Python 3.9+.
- **Флаги `mr-ready`** (без них поведение прежнее): `--auto` — Step 4 без вопросов, DISCUSS копится
  в `AR_DIR/discuss.md`; `--commit` — каждый FIX коммитится отдельно после зелёных проверок;
  `--base <branch>` — база диффа. Постинга нет и под флагами: ответы → `AR_DIR/replies.json`,
  публикует вызывающий после push.

## Step 1. Собрать замечания

SKILL_DIR — абсолютный путь из строки `Base directory for this skill: <путь>`, не cwd.
`AR_DIR` = `/tmp/audit-reply-<repo>-<branch>` — подставляй **литеральный** путь в каждую команду:
состояние шелла между вызовами `Bash` не живёт, `$VAR` не сработает. `mkdir -p AR_DIR` первым делом.

Схема треда одна для всех источников: `author`, `body`, `file` + `new_line` (может отсутствовать),
ответы. Несколько источников сразу — спроси, что разбираем, не объединяй.

### A. Ссылка на MR

```bash
python3 "SKILL_DIR/scripts/fetch_mr.py" --url "<MR_URL>" > AR_DIR/threads.json
python3 "SKILL_DIR/scripts/read_threads.py" AR_DIR/threads.json          # индекс
python3 "SKILL_DIR/scripts/read_threads.py" AR_DIR/threads.json 3 7 12   # тела целиком
```

Через файл, не в stdout: корневая нота может нести отчёт прошлого раунда (до 40К символов).
Индекс — для выбора треда, не для суждения о нём. Тела разбираемых тредов тяни целиком, одним вызовом со
всеми номерами, **без обрезки**: форму правки ревьюер диктует в хвосте треда.

JSON: мета (`source_branch`, `target_branch`, `author`) + открытые `threads`; резолвленные
пропущены (`skipped_resolved`), `--all` включает их. Фильтры: `--author <username>`, `--today`,
`--since YYYY-MM-DD`. Sanity-check: `open_total_before_filter`, `filtered_out_by_author`,
`filtered_out_by_date`.

Сводка: `N открытых тредов в MR "<title>" (<source> → <target>), пропущено резолвленных: M`.

### B. Файл-отчёт / C. Текст в инвокации

Парси сам, скрипта нет. Одно замечание = один тред:

- заголовок находки / пункт списка → `body` целиком, вместе с цитатой кода;
- `file:line` из тела → позиция; нет позиции → `is_general: true`;
- автор — из файла, иначе «ревьюер»; язык ответа — по языку замечания;
- вложенные комментарии → ответы в треде.

Сводка: `N замечаний из <путь>`.

### Общее

- B/C: границы замечания или файл неочевидны → покажи разбивку и спроси, не додумывай.
- Число не сошлось с названным пользователем → скажи о расхождении (MR — фильтр/резолв;
  файл/текст — разбивка). 0 замечаний → сообщи и остановись.

## Step 2. Сверить ветку

- `git rev-parse --abbrev-ref HEAD` против `source_branch`. Не совпало — предупреди и спроси
  (`git switch <source_branch>` или продолжить), не переключай молча.
- B/C без меты — скажи, на какой ветке стоишь, спроси базу диффа.
- База диффа: приоритет за `--base` / явно названной пользователем (часто родительская
  фича-ветка), иначе `target_branch`. `master` не хардкодь. Под `--auto` несовпадение ветки → в `discuss.md`,
  не `git switch`.
- Дифф: `git diff <base>...HEAD` (`--stat` для обзора).
- Path-scoped правила репозитория (`paths:`-фронтматтер в `.claude/rules` / `.agents/rules` /
  `.cursor/rules`) — собери **здесь**, до первого разбора:

```bash
git diff <base>...HEAD --name-only \
  | python3 "SKILL_DIR/../review-staged/scripts/match_rules.py" <repo-root> \
  > AR_DIR/rules.md
```

  Автоинъекция не канал: срабатывает только на нативный `Read` файла под глоб — не на `cat`/`sed` —
  и никогда внутри субагента. Прочитай `AR_DIR/rules.md` целиком; stderr печатает
  `matched <k> of <n>`, назови эту строку в сводке. Пустой дифф → скрипт выходит с
  `no changed files on stdin` и кодом 1: это не поломка, так и скажи — сверь базу диффа
  с пользователем. Пусто или нет папки правил — так и скажи; выдумывать правило нельзя.

## Step 3. Разобрать каждое замечание

`file` + `new_line` — отправная точка, но **номеру строки не верь**: ищи по содержимому (имя
символа, цитата кода).

Навигация — **LSP** (`hover`, `goToDefinition`, `findReferences`/`incomingCalls`,
`outgoingCalls`), греп только локатор.

**Тредов ≳4 — параллель**: субагент на тред или батч, обязательно `model: "sonnet"` отдельным
параметром в каждом вызове `Agent` (иначе наследуется opus). Дай субагенту `Read`, `Bash`
(`grep`/`rg`/`find` — тула `Grep` в сборке нет), **`LSP`** и буквальный путь
`AR_DIR/rules.md` с указанием прочитать его перед разбором. ≲3 — инлайн.

Каждый разбор:

1. **Суть** — что просит аудитор, 1–2 строки.
2. **Код по делу** — файл:строка, фрагмент, окружение (определение, вызовы, тип).
3. **Оценка** — валидно / частично / нет, почему. Опора на код и конвенции репозитория
   (собранные правила, lint-конфиг, существующие паттерны), не на вкусовщину. Правило из
   `AR_DIR/rules.md` цитируй дословно — и когда оно за аудитора, и когда против;
   пересказ по памяти правилом не является.
4. **Рекомендация** — **FIX** / **REPLY** (решено иначе / вне скоупа / аудитор ошибается /
   осознанный компромисс) / **DISCUSS** (решает пользователь). Аудитор ошибся из-за того, что код
   не показывает своего инварианта → к ответу идёт комментарий в код (Step 5).
5. **Заготовка действия** — FIX: набросок правки; REPLY: черновик ответа.

## Step 4. Решить по каждому (интерактивно)

Таблица-обзор, строка на тред:

```
| # | Файл:строка | Замечание (кратко) | Оценка | Рекомендация |
```

Дальше `AskUserQuestion`, вопрос на тред, рекомендованный вариант первым с «(рекомендую)»:

- **Править** — в описании: что и где меняем.
- **Ответить** — в описании: черновик ответа **на языке коммента**. С комментарием в код (Step 5)
  вариант называется «Ответить + пояснить в коде», текст комментария виден в описании/`preview`.
  Без этого выбора комментарий в код не вносится.
- **Обсудить** — в описании: что именно неоднозначно.

`header` ≤12 символов: `файл:строка` или `Тред N`. Патч/ответ с кодом — в `preview`.
Не переходи к Step 5 без решения по каждому треду; исключение — «по всем по рекомендации».

### `--auto`

Таблица печатается, вопросов нет: FIX и REPLY исполняются по рекомендации. DISCUSS (в
`AR_DIR/discuss.md`: тред, суть, варианты, рекомендация) — только по триггеру:

1. меняется видимое поведение или тексты UI;
2. меняется контракт API / формат данных между сервисами;
3. замечание противоречит ТЗ, или ТЗ молчит;
4. выход за скоуп тикета (кроме root-cause соседей, Step 5);
5. несогласие с **человеком**, не опровергаемое цитатой кода или правилом из `rules.md`.

Остальное — корректность, стиль, производительность, тесты — без человека. Комментарий-в-код при
REPLY — только если Step 3 назвал невидимый инвариант.

## Step 5. Выполнить согласованное

Сначала todo-список сессии (`TaskCreate`/`TodoWrite`): задача на каждый FIX, на каждый
комментарий-в-код, на оформление ответов. Статусы по ходу, одна задача в работе. Только потом
первая правка.

- **FIX** — хирургичная правка в рабочее дерево по собранным правилам и конвенциям репозитория,
  язык любой (ts/tsx/css/twig/php/go). К каждой — черновик-уведомление в тред («Поправил: …»).
  - **Чини паттерн целиком**: замечание про повторяющийся косяк → прогони весь дифф ветки, найди
    аналогичные места (`findReferences`, не только греп), покажи список, предложи поправить все.
- **REPLY** — черновик ответа: вежливо, технически, по сути, **на языке комментария**.
  Plain-текст в терминал (не блок-цитата), чтобы копировалось.
  - Ответ «код и так корректен» опирается на невидимый в коде инвариант (гарантия из вызывающего
    кода, порядок инициализации, ограничение бэкенда, неожиданный тип) → предложи **однострочный
    комментарий рядом** с тем же инвариантом: без истории правки (ни «ревьюер спрашивал», ни
    «оставлено как есть», ни ссылок на MR), один на инвариант, там, где на него натыкаются.
  - Комментарий нужен **не для всех REPLY**: вне скоупа, «поправим в следующем MR», вкусовщина,
    спор о фактах вне этого кода — не нужен, нерешённый инвариант — тикет и ссылка на него;
    инвариант выразим в коде (имя, тип, вынесенный предикат) — предложи это как FIX.
- **DISCUSS** — не закрывай сам, зафиксируй вопрос и варианты.

После всех FIX — проверка: доступные typecheck/lint/тесты либо перечитать изменённые места и их
вызовы через LSP. Правки в нескольких файлах — сверь сигнатуры с местами вызова.

### `--commit`

Проверка **после каждого FIX**: `tsc --noEmit`, `eslint`, тесты по изменённым файлам — бинарями,
не через `pnpm`/turbo. Зелено → `git add <файлы правки>` + `git commit` в стиле `git log --oneline
-10`, в теле `file:line` замечания. Красно → одна попытка; снова красно → `git checkout -- <файлы>`,
ответ в тред «попробовал, откатил: <причина>». Не `push`, не `--amend`, не `rebase`.
Комментарии-в-код для REPLY — так же, по одному.

### `AR_DIR/replies.json`

Под `--auto` — по объекту на тред с `discussion_id`:

```json
[{"discussion_id": "…", "kind": "FIX|REPLY|REVERTED", "body": "🤖 self-review · Поправил: …"}]
```

`body` начинается с `🤖 self-review · `, чтобы ревьюер отличал автоответ. Дописывать через скрипт, не heredoc-ом:

```bash
python3 "SKILL_DIR/scripts/reply_add.py" AR_DIR/replies.json <discussion_id> FIX "🤖 self-review · Поправил: …"
```

## Step 6. Итоговый отчёт

В чат; в файл `audit-reply-MR<iid>.md` — только по явной просьбе.

```markdown
## Правки (FIX)
- `<file>` — <что сделано>. Черновик в тред: «<уведомление>»

## Ответы без правок (REPLY)
### <file:line> — <автор>
> <цитата замечания>

<черновик ответа на языке комментария>
<если добавлен комментарий в код: «Плюс комментарий в `<file>`: <текст>»>

## К обсуждению (DISCUSS)
- <вопрос + варианты>
```

Напомни: правки в рабочем дереве, не закоммичены (включая комментарии к REPLY); ответы —
черновики; постинг и резолв — за пользователем. Под `--commit` — перечисли хэши коммитов и
откаченные правки; под `--auto` — путь к `replies.json` и `discuss.md`.

## Пограничные случаи

- **glab не авторизован / не тот host** — внятная ошибка + `glab auth login` (только MR).
- **Отчёт без позиций** — разбирай по смыслу диффа, позицию не выдумывай.
- **Замечание не находится в коде** — отчёт устарел или по другой ветке: скажи и спроси, не
  подгоняй под похожее место.
- **Номер строки устарел** — ищи по содержимому, отметь расхождение в разборе.
- **`is_general: true`** — якоря нет, разбирай по смыслу всего диффа.
- **`is_author_self: true`** — заметка автора, не замечание: контекст или пропуск. Кроме тел с
  `🤖 self-review ·` — это находки `mr-ready`, разбирай как замечания.
- **Несколько ревьюеров** — атрибутируй ответы по автору треда.
- **`reply_count > 0`** — учитывай переписку, не повторяй сказанное.
